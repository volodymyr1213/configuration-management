---
 - name: Intall Apache on Ubuntu
 - hosts: all
   become_method: yes
   tasks:
      - name: Install apache
        apt:
          name: apache2
          state: present
        when: ansible_distribution == "Ubuntu"
            