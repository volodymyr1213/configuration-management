---
  - name: Create files older than 90 days
    hosts: all
    tasks: 
      - name: Create files
        file: 
          name: "{{ item }}"
         # path: /tmp/{{ item('pipe', 'date +20180511-1212')}}
          state: touch
         # access_time: 201805010923.15
      - set_fact: mydate="{{lookup('pipe','date +20180511-1212')}}"
        loop:
           - shermat
           - toshmat
           - nurmat
           - shermamat
           - mamatqul
        when: ansible_distribution == "CentOS"
        