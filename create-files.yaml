---
  - name: Create files older than 90 days
    hosts: all
    tasks: 
      - name: Create files
        file: 
          name: "{{ item }}"
          path: /etc/foo.conf
          state: touch
          access_time: 20180501
        loop:
           - shermat
           - toshmat
           - nurmat
           - shermamat
           - mamatqul
        when: ansible_distribution == "CentOS"
        