---
  - name: Create files older than 90 days
    hosts: all
    tasks:
      - name: Create files
        file:
          path: /tmp/file{1..5}
          state: touch
          access_time: 201805010923.15  #YYYYMMDDHHMM.SS
        when: ansible_distribution == "CentOS"
